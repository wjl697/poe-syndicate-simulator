# 辛迪加遭遇战右侧选项机制与规则文档

本文件基于游戏核心代码的实际实现，详细剖析遭遇战面板中**最右侧选项**（处决、背叛、商谈）的触发条件、效果池构成、出现概率（权重）以及各效果的底层执行细节。

***

## 一、 右侧选项的触发判定条件

在遭遇战中，当玩家悬停或选中某张待处理卡牌时，系统会根据当前遭遇战**尚未处理（未做出选择）的存活人数**来决定最右侧按钮显示的动作类型：

| 剩余未处理人数   | 关系与身份判定                                         | 触发的最右侧动作                                                        |
| :-------- | :---------------------------------------------- | :-------------------------------------------------------------- |
| **≥ 3 人** | 忽略任何关系与身份                                       | **【处决】 (EXECUTE)**                                              |
| **= 2 人** | 双方在数据中存在 **TRUST (信任 / 绿线)** 关系                 | **【背叛】 (BETRAY)**                                               |
| **= 2 人** | 无信任关系，但双方在同部门呈 **上下级关系** (Leader + Subordinate) | 当前卡为 **部下**：**【背叛】 (BETRAY)**<br>当前卡为 **首领**：**【处决】 (EXECUTE)** |
| **= 2 人** | 跨部门无关系，或双方为 **RIVALRY (敌对 / 红线)** 关系            | **【处决】 (EXECUTE)**                                              |
| **= 1 人** | 忽略任何关系与身份                                       | **【商谈】 (BARGAIN)**                                              |

> \[!NOTE]
>
> 1. 同部门上下级的判定基准是：双方均有部门（非 Division.NONE）、部门相同，且 `is_leader` 属性一个为 `true` 另一个为 `false`。
> 2. 当人数为 2 时，**信任关系判定优先级高于上下级关系**。即如果部门首领与下属结为“信任”，首领最右侧选项也会被覆盖为“背叛”。

***

## 二、 三大动作的详细效果与随机池权重

每场遭遇战生成或状态更新时，系统会提前为候选卡牌预先随机抽签（Pre-roll）并缓存结果。随机池中所有的候选效果均采用**等概率（等权重）均分**的规则进行抽取。

***

### 1. 【处决】 (EXECUTE)

处决没有随机效果池，其执行结果是**100% 确定**的：

* **普通升星**：如果当前卡牌星级 `< 3`，则星级 `+1`。若已是 3 星，则显示“已是最高等级，无效果”。

* **自由人招募**：如果当前被处决的人原先是 **0星自由人**（即星级为 0 且部门为 `NONE`）：

  * 他会试图加入当前遭遇战所属的部门。

  * **判定加入**：如果该部门当前总人数（首领 + 部下）`< 5` 人，则他会成功加入该部门，并正常获得星级 `+1` 变为 1 星。

  * **部门已满**：如果该部门人数已达上限 5 人，则招募失败，他仍保持为 0 星自由人，且不执行升星。

***

### 2. 【背叛】 (BETRAY)

背叛的候选效果池根据当前卡牌（动作执行者）与另一名未处理成员（目标）的身份状态动态组装：

#### 效果池组装规则：

* **基础池**（默认一定加入）：

  1. `INCREASE_INTEL_MAKE_RIVALRY` (增加情报并结仇)
  2. `REMOVE_FROM_SYNDICATE` (逐出辛迪加)

* **条件追加 1**（双方为同部门上下级）：

  * 追加 `STEAL_RANK` (窃取等级并结仇)

* **条件追加 2**（双方属于不同部门，且均不是自由人）：

  * 追加 `RAISE_OWN_DIV_LOWER_OTHER_DIV` (提升自身部门军衔，降低对方部门军衔)

  * 追加 `DESTROY_OTHER_DIV_EQUIP` (摧毁对方部门装备)

  * 追加 `GAIN_OTHER_DIV_INTEL` (获取对方部门情报)

#### 出现概率：

* **无同部门/跨部门关系时**（池中 2 项）：每项出现概率为 **50.0%**。

* **同部门上下级时**（池中 3 项）：每项出现概率为 **33.3%**。

* **跨部门成员相遇时**（池中 5 项）：每项出现概率为 **20.0%**。

#### 详细执行效果：

| 效果类型                                | 说明与具体执行逻辑                                                                                                          |
| :---------------------------------- | :----------------------------------------------------------------------------------------------------------------- |
| **`INCREASE_INTEL_MAKE_RIVALRY`**   | 自身所在的部门获得 **15 点** 情报。同时自身与目标的关系变更为 **RIVALRY (敌对)**。                                                              |
| **`REMOVE_FROM_SYNDICATE`**         | 目标卡牌被永久逐出辛迪加（`is_on_board = false`），星级重置为 1，装备清空。如果目标是首领则会自动下台。随后从替补席（未上盘的成员）中随机选一人作为 **0星自由人** 新加入辛迪加棋盘。          |
| **`STEAL_RANK`**                    | 自身所在部门获得 **15 点** 情报。自身直接吸取目标的全部星级（自身最高升至 3 星），目标星级重置为 0，失去当前职务与部门（降为自由人）。如果目标原本是首领则自动下台。双方关系变更为 **RIVALRY (敌对)**。 |
| **`RAISE_OWN_DIV_LOWER_OTHER_DIV`** | 自身部门所有成员（首领 + 所有部下）星级 `+1`（最高 3 星）。目标部门全员星级 `-1`（最低 0 星，降到 0 星的成员自动脱离部门降为自由人，若降级的是首领则首领自动下台）。执行后**清除**双方的关系（变为中立）。 |
| **`DESTROY_OTHER_DIV_EQUIP`**       | 彻底清空目标所在部门所有成员（首领 + 部下）身上的辛迪加装备（装备数设为 0）。执行后**清除**双方的关系（变为中立）。                                                     |
| **`GAIN_OTHER_DIV_INTEL`**          | 直接获取目标所在部门的 **20 点** 情报。执行后**清除**双方的关系（变为中立）。                                                                      |

***

### 3. 【商谈】 (BARGAIN)

商谈仅在只剩 1 人时触发，其效果池非常庞大，且受到盘面实时状态的强烈制约。

#### 效果池组装规则：

* **基础池**（默认一定加入）：

  1. `GAIN_OWN_DIV_INTEL` (获取自身部门情报)
  2. `DROP_RANDOM_ITEM` (掉落随机物品)
  3. `REMOVE_FROM_SYNDICATE` (自身退出辛迪加)
  4. `DESTROY_OWN_DIV_EQUIP` (摧毁自身部门装备以求饶)

* **条件追加项**：

  * **追加 5**：如果在本次遭遇战中，上一个刚刚被处理的成员与当前商谈者在数据上是 **RIVALRY (敌对)** 关系：

    * 追加 `GAIN_RIVAL_DIV_INTEL` (获取仇敌部门情报并和解)

  * **追加 6**：除自己之外，棋盘上存在至少 1 名已翻开的存活成员：

    * 追加 `FORM_TRUST_ANY_GAIN_INTEL` (与随机成员结盟)

  * **追加 7**：棋盘上存在已翻开的 0星自由人，且商谈者有部门，且商谈者部门人数 `< 5`：

    * 追加 `RECRUIT_FREE_AGENT` (招募自由人入部)

  * **追加 8**：棋盘上存在已翻开且未在押的非首领成员，其星级与当前商谈者**相同**（且商谈者星级 `> 0`），同时商谈者**不是首领**，目标与其部门不同：

    * 追加 `SWAP_DIVISION` (调换部门与职务)

  * **追加 9**：当前有任何成员正在监狱中服刑（`prison_queue.size() > 0`）：

    * 追加 `COMPLETE_INTERROGATIONS` (释放全部囚犯)

  * **追加 10**：商谈者有部门，且商谈者所在部门内有任何成员身上挂有 **RIVALRY (红线)** 关系：

    * 追加 `REMOVE_ALL_RIVALRIES_IN_DIV` (清空部门红线)

#### 出现概率：

商谈效果池的大小在 **4 \~ 10** 之间浮动。根据符合条件的追加项数量，池内所有有效选项**等概率平分**。例如如果当前满足 2 个追加条件，池中共有 6 个选项，则每个选项被 Roll 到的概率为 **16.67%**。

#### 详细执行效果：

| 效果类型                              | 说明与具体执行逻辑                                                                                         |
| :-------------------------------- | :------------------------------------------------------------------------------------------------ |
| **`GAIN_OWN_DIV_INTEL`**          | 自身所属的部门获得 **20 点** 情报。                                                                            |
| **`DROP_RANDOM_ITEM`**            | 在 `["加密物品", "圣甲虫", "通货物品", "传奇物品", "地图"]` 奖励表里等概率随机掉落一件物品给玩家。                                     |
| **`REMOVE_FROM_SYNDICATE`**       | 自身离开不朽辛迪加，重置回替补席。原职务下台，并从替补席随机调入一个新人作为 0 星自由人加入盘面。                                                |
| **`DESTROY_OWN_DIV_EQUIP`**       | 作为求饶条件，清空自身部门内所有成员身上的全部装备。                                                                        |
| **`GAIN_RIVAL_DIV_INTEL`**        | 获得上一个被处理的仇敌所属部门的 **25 点** 情报，同时斩断与该仇敌的敌对关系（变回中立）。                                                 |
| **`FORM_TRUST_ANY_GAIN_INTEL`**   | 随机在盘面上挑选一个除自己之外的已翻开成员：如果和他原本是敌对，则冰释前嫌变为中立；如果是其他关系，则直接结为 **TRUST (信任 / 绿线)**，并获得目标部门的 **15 点** 情报。 |
| **`RECRUIT_FREE_AGENT`**          | 在盘面已翻开的 0 星自由人中随机挑一人招募进自己的部门，其星级升为 1 星。如果原本与他是仇敌则化解为中立；否则直接与他结为 **TRUST (信任)** 关系。                |
| **`SWAP_DIVISION`**               | 在盘面上寻找一个与自己星级相同的跨部门非首领成员，双方互换所在的部门以及在部门中的职位（部下互换）。                                                |
| **`COMPLETE_INTERROGATIONS`**     | **立即提前释放监狱里的所有囚犯**。每位囚犯会把他们入狱时计算的“每回合产出情报”乘以“剩余服刑回合数”作为补偿，立即折算为情报点数累加给他们各自对应的部门，随后出狱。             |
| **`REMOVE_ALL_RIVALRIES_IN_DIV`** | 遍历自身部门的所有成员，彻底斩断并抹除所有与他们相连的 **RIVALRY (敌对 / 红线)** 关系。                                             |

***

## 三、 首领退位与自动提拔机制

在进行处决、背叛或商谈操作时，经常会触发**首领下台（Step Down）或**首领被永远逐出辛迪加（Kick）。当某个部门的首领位置空缺时，系统会**严格按照以下优先级顺序**进行自动提拔：

1. **同部门部下顺位提拔（军衔最高者优先）**：
   查找同部门内所有存活且未在押的部下，按星级降序排序，提拔星级最高的那个部下为新首领（`is_leader = true`）。
2. **自由人招募提拔**：
   如果部门内没有其他部下，则从棋盘上的非在押自由人中随机挑选一人，直接加入该部门并提拔为新首领。其星级如果为 0，会强行升至 1 星。
3. **跨部门调任提拔**：
   如果没有任何自由人可用，则从其他部门中随机调任一名成员加入该部门担任首领。

   * **调任池优先级**：优先从其他部门的非首领（部下）中随机选；若其他部门只剩首领，则只能从其他部门的首领中调任。

   * **重置惩罚**：被调任的成员在新部门担任首领，但其**星级会被强行重置作废，降为 1 星**。

   * **连锁提拔**：如果调任的成员在原部门是首领，他的离去会导致原部门首领空缺，原部门会立即进入这套提拔流程重新提拔一个首领。
4. **首领空缺（无可提拔人员）**：
   如果盘面上没有任何可用的人选满足上述条件，该部门的首领位置将暂时保持空缺状态。

