# Task List: Sandbox Mode Setup Wizard and Integration

- [x] Create `task.md` to organize the work
- [x] Create `implementation_plan.md` to formulate the design
- [x] Implement changes in `scripts/autoloads/game_manager.gd`
  - Add `var bench_pool: Array[String] = []` to track the bench members globally.
  - Initialize and update `bench_pool` in `initialize_game()`, `_assign_members_randomly()`, `save_state()`, and `undo()`.
  - Add a sandbox initialization helper: `func initialize_sandbox_mode(active_names: Array[String])`.
  - Update `_kick_member_and_replace()` to draw from and push back to `bench_pool`.
- [x] Implement changes in `scripts/gameplay/action_logic.gd`
  - Add helper functions to fetch all betray/bargain effects with names, descriptions, and validity flags.
  - Update replacement logic to interface with the global `GameManager.bench_pool`.
- [x] Implement changes in `scripts/ui/card_action_overlay.gd`
  - Update overlay panel creation: in sandbox mode, if the action is `BETRAY` or `BARGAIN`, display all options (valid ones enabled, invalid ones greyed out/disabled) in a scrollable list, allowing the player to choose.
  - Set the chosen effect and target in the member's cache right before calling the execution callback.
- [x] Create the new setup wizard script `scripts/ui/sandbox_setup_wizard.gd`
  - Define the UI Control Node layout:
    - Step 1: Select 14 active members (grid/list of 17 with checkboxes/selection).
    - Step 2: Assign those 14 members to board slots by clicking a card in the wizard list, then clicking a slot Rect on the board.
    - Step 3: Draw relationship lines by toggling green (Trust), red (Rivalry), or clear (Neutral) buttons and clicking card pairs on the board.
  - Display board slots (highlighting and enabling click input on `leader_*` and `*slot` ColorRects).
  - Bind custom click handlers to slot ColorRects.
  - Support resetting / clearing layout, and a "Complete" button.
- [x] Integrate the wizard in `scripts/main.gd`
  - Modify `_on_sandbox_pressed()` to open/close the sandbox wizard.
  - Correctly clean up or pause standard encounter flows when sandbox mode is enabled.
- [x] Verify compilation headlessly and fix any syntax/runtime issues.
- [x] Write `walkthrough.md` detailing the changes and user instructions.
